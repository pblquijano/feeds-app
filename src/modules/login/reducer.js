// import update from 'immutability-helper';

const INITIAL_STATE = {
    initialValues:{
        email: '',
        password: ''
    }
}

export default function(state = INITIAL_STATE) {
    return state;
}